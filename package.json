{
  "name": "ha-interactive-floorplan",
  "private": true,
  "version": "1.1.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "build:card": "vue-tsc -b && vite build -c vite.config.card.ts",
    "build:editor": "vue-tsc -b && vite build -c vite.config.editor.ts",
    "build:combined": "vue-tsc -b && vite build -c vite.config.combined.ts",
    "build:all": "npm run build:card && npm run build:editor",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^4.1.1",
    "pinia": "^3.0.4",
    "uuid": "^13.0.0",
    "vue": "^3.5.24",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/uuid": "^10.0.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vue-tsc": "^3.1.4"
  }
}